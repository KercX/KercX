-- Squid Game Full Local GUI
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local Lighting = game:GetService("Lighting")

-- ===== ScreenGui =====
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- ===== Helpers =====
local function addCorner(obj,r)
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0,r or 8)
    c.Parent = obj
end

local function makeDraggable(gui)
    local dragging, dragInput, dragStart, startPos
    gui.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = gui.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging=false end
            end)
        end
    end)
    gui.InputChanged:Connect(function(input)
        if input.UserInputType==Enum.UserInputType.MouseMovement then dragInput=input end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input==dragInput and dragging then
            local delta = input.Position - dragStart
            gui.Position = UDim2.new(startPos.X.Scale,startPos.X.Offset + delta.X,startPos.Y.Scale,startPos.Y.Offset + delta.Y)
        end
    end)
end

-- ===== Open Button =====
local openBtn = Instance.new("TextButton")
openBtn.Size = UDim2.new(0,160,0,50)
openBtn.Position = UDim2.new(0,20,0,20)
openBtn.BackgroundColor3 = Color3.fromRGB(50,50,50)
openBtn.Text = "INK GAME"
openBtn.TextColor3 = Color3.fromRGB(255,255,255)
openBtn.Font = Enum.Font.FredokaOne
openBtn.TextScaled = true
openBtn.Parent = screenGui
addCorner(openBtn,12)
makeDraggable(openBtn)

-- ===== Main Frame =====
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0,420,0,300)
frame.Position = UDim2.new(0.5,-210,0.5,-150)
frame.BackgroundColor3 = Color3.fromRGB(30,30,30)
frame.Visible = false
frame.Parent = screenGui
addCorner(frame,12)
makeDraggable(frame)

openBtn.MouseButton1Click:Connect(function()
    if frame.Visible then
        TweenService:Create(frame,TweenInfo.new(0.3,Enum.EasingStyle.Quad),{Size=UDim2.new(0,0,0,0)}):Play()
        task.wait(0.3)
        frame.Visible = false
        frame.Size = UDim2.new(0,420,0,300)
    else
        frame.Size = UDim2.new(0,0,0,0)
        frame.Visible = true
        TweenService:Create(frame,TweenInfo.new(0.3,Enum.EasingStyle.Quad),{Size=UDim2.new(0,420,0,300)}):Play()
    end
end)

-- ===== Categories Left Frame =====
local categoriesFrame = Instance.new("Frame")
categoriesFrame.Size = UDim2.new(0,130,1,0)
categoriesFrame.Position = UDim2.new(0,0,0,0)
categoriesFrame.BackgroundColor3 = Color3.fromRGB(40,40,40)
categoriesFrame.Parent = frame
addCorner(categoriesFrame,8)

local layout = Instance.new("UIListLayout",categoriesFrame)
layout.Padding = UDim.new(0,5)

-- ===== Content Frame =====
local contentFrame = Instance.new("Frame")
contentFrame.Size = UDim2.new(1,-140,1,-10)
contentFrame.Position = UDim2.new(0,140,0,5)
contentFrame.BackgroundColor3 = Color3.fromRGB(25,25,25)
contentFrame.Parent = frame
addCorner(contentFrame,8)

-- ===== Pages =====
local pages = {}
local function newCategory(name)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1,-10,0,30)
    btn.BackgroundColor3 = Color3.fromRGB(60,60,60)
    btn.Text = name
    btn.TextColor3 = Color3.fromRGB(255,255,255)
    btn.Font = Enum.Font.Gotham
    btn.TextScaled = true
    btn.Parent = categoriesFrame
    addCorner(btn,6)

    local page = Instance.new("Frame")
    page.Size = UDim2.new(1,0,1,0)
    page.BackgroundTransparency = 1
    page.Visible = false
    page.Parent = contentFrame
    pages[name] = page

    btn.MouseButton1Click:Connect(function()
        for _,p in pairs(pages) do p.Visible = false end
        page.Visible = true
    end)

    return page
end

local function makeButton(parent,text,callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0,250,0,35)
    btn.Position = UDim2.new(0,10,0,#parent:GetChildren()*40)
    btn.BackgroundColor3 = Color3.fromRGB(70,70,70)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255,255,255)
    btn.Font = Enum.Font.Gotham
    btn.TextScaled = true
    btn.Parent = parent
    addCorner(btn,6)
    btn.MouseButton1Click:Connect(callback)
end

-- ===== CATEGORY FUNCTIONS =====
-- 1. Green Light
local cat1 = newCategory("Green Light")
makeButton(cat1,"TP To End",function()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(1000,5,1000)
    end
end)
makeButton(cat1,"GodMode",function()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.Health = math.huge
    end
end)
makeButton(cat1,"Help Players",function() print("Help Players") end)
makeButton(cat1,"Troll Players",function() print("Troll Players") end)

-- 2. Dalgona
local cat2 = newCategory("Dalgona")
makeButton(cat2,"Auto Complete",function() print("Auto Complete") end)
makeButton(cat2,"AntiFling",function()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        LocalPlayer.Character.HumanoidRootPart.AssemblyLinearVelocity = Vector3.new()
        print("AntiFling")
    end
end)

-- 3. Hide & Seek
local cat3 = newCategory("Hide & Seek")
local espActive=false
local espParts = {}
makeButton(cat3,"ESP ON",function()
    espActive=true
end)
makeButton(cat3,"ESP OFF",function()
    espActive=false
    for _,p in pairs(espParts) do p:Destroy() end
    espParts={}
end)
makeButton(cat3,"Kill Hider",function()
    print("Kill Hider clicked")
end)
makeButton(cat3,"TP & Kill",function()
    print("TP & Kill clicked")
end)
-- ESP loop
RunService.RenderStepped:Connect(function()
    if espActive then
        for _,plr in pairs(Players:GetPlayers()) do
            if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                if not espParts[plr] then
                    local box = Instance.new("BoxHandleAdornment")
                    box.Adornee = plr.Character.HumanoidRootPart
                    box.Size = plr.Character.HumanoidRootPart.Size
                    box.Color = Color3.fromRGB(255,0,0)
                    box.AlwaysOnTop = true
                    box.Parent = plr.Character.HumanoidRootPart
                    espParts[plr]=box
                end
            end
        end
    end
end)

-- 4. Tug Of War
local cat4 = newCategory("Tug Of War")
makeButton(cat4,"Auto TAP",function()
    print("Auto TAP ON")
end)

-- 5. JumpRope
local cat5 = newCategory("JumpRope")
makeButton(cat5,"TP To End",function()
    print("JumpRope TP To End")
end)
makeButton(cat5,"Help Player",function() print("Help Player") end)

-- 6. Glass Bridge
local cat6 = newCategory("Glass Bridge")
local glassESP=false
makeButton(cat6,"ESP Glass ON",function() glassESP=true end)
makeButton(cat6,"ESP Glass OFF",function() glassESP=false end)
makeButton(cat6,"TP To End",function() print("Glass Bridge TP") end)

-- 7. Mingle
local cat7 = newCategory("Mingle")
local noclip=false
local antikill=false
makeButton(cat7,"Noclip",function() noclip=not noclip end)
makeButton(cat7,"AntiKill",function() antikill=not antikill end)

-- 8. LocalPlayer
local cat8 = newCategory("LocalPlayer")
local speed=16
local powerjump=false
local noclipLP=false
local antikillLP=false
makeButton(cat8,"Speed +10",function() speed=speed+10 end)
makeButton(cat8,"Speed Reset",function() speed=16 end)
makeButton(cat8,"Noclip",function() noclipLP=not noclipLP end)
makeButton(cat8,"AntiKill",function() antikillLP=not antikillLP end)
makeButton(cat8,"PowerJump",function() powerjump=not powerjump end)

RunService.Stepped:Connect(function()
    if LocalPlayer.Character then
        local hrp=LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        local hum=LocalPlayer.Character:FindFirstChild("Humanoid")
        if hrp then
            hrp.AssemblyLinearVelocity = Vector3.new()
            if noclipLP then
                for _,part in pairs(LocalPlayer.Character:GetChildren()) do
                    if part:IsA("BasePart") then part.CanCollide=false end
                end
            end
            if hum and antikillLP then
                hum.Health = math.huge
            end
            if powerjump then
                hum.JumpPower = 150
            else
                hum.JumpPower = 50
            end
            hum.WalkSpeed = speed
        end
    end
end)

-- 9. Rebel
local cat9 = newCategory("Rebel")
makeButton(cat9,"AutoWin",function() print("AutoWin ON") end)

-- ===== Avatar & Testers =====
local catAvatar = newCategory("Avatar & Testers")
makeButton(catAvatar,"Kerc912",function() print("Avatar Kerc912") end)
makeButton(catAvatar,"Brat1shka",function() print("Avatar Brat1shka") end)
makeButton(catAvatar,"Random Testers",function() print("Random players tested") end)
